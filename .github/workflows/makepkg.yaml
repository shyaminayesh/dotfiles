name: makepkg

on:
  workflow_dispatch:

jobs:

  makepkg:
    name: makepkg
    runs-on: ubuntu-latest
    steps:

      - name: checkout
        uses: actions/checkout@v2

      - name: printsrc
        uses: enkodin/action-archlinux-makepkg@dev
        with:
          cmd: "--printsrc"